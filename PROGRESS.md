# PROGRESS — оперативный журнал текущей сессии

> Этот файл — снимок состояния "здесь и сейчас", на случай обрыва сессии по лимиту токенов.
> Общий план по этапам и завершённая история — в `CLAUDE.md`. Здесь — только актуальный срез:
> что в работе прямо сейчас и в каком именно состоянии.
> Обновлять после каждого значимого шага (законченная подзадача, архитектурное решение, найденная проблема).
> Работа ведётся внутри `.claude/worktrees/pwa-rebuild/` (см. `CLAUDE.md` → "Где реально идёт разработка").

---

## Текущая задача

Этап 5 плана (`CLAUDE.md`) — «Доска + ввод хода»: собрать компоненты доски, экранной клавиатуры и модалок, свести их во вкладке «Ход» в `App.tsx`.

## Сделано

- [x] `src/game/constants.ts` — добавлен экспорт `ALPHABET_ORDERED` (32 буквы, алфавитный порядок для LetterPad)
- [x] `src/game/board-layouts.ts` — добавлена `letterMultiplierAt()` (3/2/1 для Л3/Л2/обычных клеток)
- [x] `tests/game/constants.test.ts` — тесты на новые константы
- [x] `src/components/board/BoardCell.tsx` (68 строк) — клетка доски v2, `cqw`-адаптивный шрифт, Lucide Target для центра
- [x] `src/components/board/Board.tsx` (71 строка) — доска 15×15 CSS Grid + Zustand-стор
- [x] `src/components/rack/LetterPad.tsx` (50 строк) — экранная кириллическая клавиатура, джокер ★, backspace
- [x] `src/components/modals/ExchangeLetterModal.tsx` (110 строк) — модалка обмена буквы на shadcn `Dialog`

Все перечисленные файлы созданы/изменены, но **ещё НЕ закоммичены** (рабочая копия worktree) и **ещё НИ РАЗУ не прогонялись** через tsc/vitest/build/lint после создания.

## В процессе (на чём именно остановились)

- [ ] Проверка API `src/components/ui/dialog.tsx` (`grep -n "^function\|^export"`) — последнее действие прерванной сессии, судя по всему, шло к созданию следующей модалки (`JokerExchangeModal.tsx`) по образцу уже готового `ExchangeLetterModal.tsx`. Сам файл `JokerExchangeModal.tsx` ещё не создан.

## Осталось

- [ ] Создать `JokerExchangeModal.tsx` (по образцу `ExchangeLetterModal.tsx`)
- [ ] Создать `InsufficientLettersModal.tsx`
- [ ] Создать `RackEditor.tsx`
- [ ] Создать `PreviewStrip.tsx`
- [ ] Создать `MoveEntryPanel.tsx`
- [ ] Создать `ScoreBreakdown.tsx`
- [ ] Свести `Board`, `LetterPad`, `RackEditor`, `MoveEntryPanel` и все три модалки во вкладке «Ход» в `App.tsx` (сейчас там плейсхолдер-контент, коммит `a237776`)
- [ ] Прогнать `npx tsc -b`, `npx vitest run`, `npm run build`, `npx oxlint src` — обязательно перед коммитом Этапа 5
- [ ] Закоммитить Этап 5 отдельным коммитом (только после зелёных проверок)
- [ ] Обновить `CLAUDE.md`: пометить Этап 5 как ✅ выполненный, добавить номер коммита
- [ ] Дальше по плану — Этапы 6–10 (Подсказки → Мешок/История → Статистика множителей → PWA/деплой → полировка), см. `CLAUDE.md`

## Решения и договорённости

- Работать только внутри `.claude/worktrees/pwa-rebuild/` — корень репозитория (`main`) кода не содержит.
- Не читать целиком `russian_words_nouns.txt` (все 3 копии) и `Chat_history.md` — см. правило в `CLAUDE.md`.
- Компоненты `game/`, `dictionary/`, `engine/` не импортируют React — не нарушать эту границу при добавлении новых UI-компонентов.
- shadcn/ui в проекте на `@base-ui/react`, а не Radix — при работе с `Dialog`/`Tooltip`/`Popover` сверяться с реальным API компонента в `src/components/ui/*`, а не с документацией Radix.
- Каждый этап коммитится отдельно и только после зелёных `tsc -b` + `vitest run` + `build` (см. историю коммитов в `CLAUDE.md`).
- После завершения Этапа 5 — синхронизировать статус в `CLAUDE.md` (галочка + номер коммита) и сбросить секции «В процессе»/«Осталось» этого файла на начало Этапа 6.
